version: 0.1
component: build
shell: bash

steps:
  - type: Command
    name: "Upsert OIC Library (PUT if exists, POST if not) — OAuth"
    timeoutInSeconds: 1200
    command: |
      set -euo pipefail

      # ===== Required parameters =====
      : "${OIC_HOST:?Missing OIC_HOST}"                # https://design.integration.<region>.ocp.oraclecloud.com
      : "${OIC_INSTANCE_ID:?Missing OIC_INSTANCE_ID}"  # Instance short name (About > Service instance)
      : "${LIB_NAME:?Missing LIB_NAME}"                # e.g. XX_COMMON_UTILS
      : "${LIB_FILE:?Missing LIB_FILE}"                # Full path to library archive (.zip)
      : "${OAUTH_TOKEN_URL:?Missing OAUTH_TOKEN_URL}"
      : "${OAUTH_CLIENT_ID:?Missing OAUTH_CLIENT_ID}"
      : "${OAUTH_CLIENT_SECRET:?Missing OAUTH_CLIENT_SECRET}"
      : "${OAUTH_SCOPE:=}"                             # optional

      [ -f "$LIB_FILE" ] || { echo "❌ Library file not found: $LIB_FILE"; exit 1; }

      echo ">> OIC_HOST        = $OIC_HOST"
      echo ">> OIC_INSTANCE_ID = $OIC_INSTANCE_ID"
      echo ">> LIB_NAME        = $LIB_NAME"
      echo ">> LIB_FILE        = $LIB_FILE"

      BASE="$OIC_HOST/ic/api/integration/v1"

      # ===== Get OAuth2 token =====
      echo "== Requesting OAuth2 token =="
      TOKEN=$(curl -s -X POST "$OAUTH_TOKEN_URL" \
        -H "Content-Type: application/x-www-form-urlencoded" \
        -d "grant_type=client_credentials&client_id=$OAUTH_CLIENT_ID&client_secret=$OAUTH_CLIENT_SECRET&scope=$OAUTH_SCOPE" \
        | python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))")
      [ -n "$TOKEN" ] || { echo "❌ Could not obtain access_token"; exit 1; }

      echo "✅ Token obtained successfully"

      # ===== Check if library exists =====
      echo "== Checking if library '$LIB_NAME' exists =="
      EXISTS_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
        -H "Authorization: Bearer $TOKEN" \
        "$BASE/libraries/${LIB_NAME}?integrationInstance=${OIC_INSTANCE_ID}" || true)

      echo ">> Library existence HTTP code: $EXISTS_CODE"

      # ===== PUT (replace) if exists =====
      if [ "$EXISTS_CODE" = "200" ]; then
        echo "== Library exists → REPLACING via PUT =="
        RESP_FILE="$(mktemp)"
        HTTP_CODE=$(curl -s -S -o "$RESP_FILE" -w "%{http_code}" \
          -H "Authorization: Bearer $TOKEN" \
          -X PUT "$BASE/libraries/archive?integrationInstance=${OIC_INSTANCE_ID}" \
          -F "file=@${LIB_FILE}" \
          -F "type=application/octet-stream" || true)

        if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "204" ]; then
          echo "✅ Library replaced successfully — HTTP $HTTP_CODE"
        else
          echo "❌ PUT failed — HTTP $HTTP_CODE"
          sed -n '1,200p' "$RESP_FILE" || true
          rm -f "$RESP_FILE"
          exit 1
        fi
        rm -f "$RESP_FILE"

      # ===== POST (create) if not found =====
      elif [ "$EXISTS_CODE" = "404" ]; then
        echo "== Library not found → CREATING via POST =="
        RESP_FILE="$(mktemp)"
        HTTP_CODE=$(curl -s -S -o "$RESP_FILE" -w "%{http_code}" \
          -H "Authorization: Bearer $TOKEN" \
          -X POST "$BASE/libraries/archive?integrationInstance=${OIC_INSTANCE_ID}" \
          -F "file=@${LIB_FILE}" \
          -F "type=application/octet-stream" || true)

        if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ] || [ "$HTTP_CODE" = "204" ]; then
          echo "🆕 Library created successfully — HTTP $HTTP_CODE"
        else
          echo "❌ POST failed — HTTP $HTTP_CODE"
          sed -n '1,200p' "$RESP_FILE" || true
          rm -f "$RESP_FILE"
          exit 1
        fi
        rm -f "$RESP_FILE"

      else
        echo "❌ Unexpected response code while checking library ($EXISTS_CODE)"
        exit 1
      fi

      echo "✅ Library upsert completed successfully"
